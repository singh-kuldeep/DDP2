////////////////////////////////
// Command to run the code
// g++ TVDmainsolver.cpp -std=c++11
// 
// For post processing 
// python plot.py
///////////////////////////////

// Input file 
// Don't forget to put "=" 
// Comments should be in a separate line 
// keep only ONE space

// 1. CFL 
// CFL = 0.008
// CFL = 1.01
// CFL = 0.1
CFL = 0.5

// 2. Total number of iterations (TotalIteration)
TotalIteration = 1500000

// 3. There are two options available for scheme 
// Scheme = Roe
Scheme = AUSM

// 4. There are two options available for gamma 
gamma = Constant
// gamma = Gamma(T)

// 4. Free stream parameters at inlet
// TemperatureFreestream = 3000
// PressureFreestream = 3e5
// MachFreestream = 2  
// MachFreestream =  0.4521  
// MachFreestream =  0.2817  


// 5. Boundary condition
// Options for BC 
// 1. SuperSonicInlet
// All the five quantities needs to be specified because all the waves are going in
InletDensity = 1.23
InletXVelocity = 829
InletYVelocity = 0
InletZVelocity = 0
InletStaticPressure = 529379.74 

// 2. SuperSonicExit 
// No need to define anything simple extrapolation will be used 

// 3. SubSonicInlet
// Any four quantities needs to be specified because 4 waves are going in 
// InletDensity = 1.23
// InletXVelocity = 1000
// InletYVelocity = 0.0
// InletZVelocity = 0.0

// 4. SubSonicExit
// Any one quantities needs to be specified because 1 waves are going in
// ExitStaticPressure = 1e5  

// 5. BC options according to the location
// SubSonicInlet : It is implemented using the NRBC
// SuperSonicInlet : It is implemented by imposing the total quantities
// SubSonicExit : It is implemented using the NRBC
// SuperSonicExit : It is implemented by simple first order extrapolation
// Wall : It is implemented by making the normal compoment of the velocity zero

BoundaryConditionati0 = SubSonicInlet
// BoundaryConditionati0 = SuperSonicInlet
BoundaryConditionatj0 = Wall
BoundaryConditionatk0 = Wall
BoundaryConditionatiNi = SuperSonicExit
BoundaryConditionatjNj = Wall
BoundaryConditionatkNk = Wall

// 7. Initial Condition options
// InitialCondition = ZeroVelocity
// InitialCondition = FreeStreamParameterAndZeroVelocity
// InitialCondition = FreeStreamParameterEverywhare
InitialCondition = NozzleBasedOn1DCalculation
// InitialCondition = StartFromPreviousSolution

// 8. Geometry options
// GeometryOption = StraightDuct
// GeometryOption = BumpInsidetheStraightSuct
GeometryOption = IdelNozzleDesignedUsingMOC

// 9. Local time steping
// TimeSteping = Local
TimeSteping = Global